name: riot_api
description: A package for using the Riot API in Dart language.
version: 0.1.0
homepage: https://github.com/your_username/riot_api

environment:
  sdk: ^3.0.0

dependencies:
  http: ^0.13.5
  json_annotation: ^4.8.0
  plugin_platform_interface: ^2.0.2
  # Add your dependencies here

dev_dependencies:
  json_serializable: ^6.6.1
  build_runner: ^2.3.3
  lints: ^2.0.0
  test: ^1.21.0
  # Add your dev dependencies here
